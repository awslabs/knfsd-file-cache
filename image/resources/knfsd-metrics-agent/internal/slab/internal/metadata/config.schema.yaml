# Code generated by mdatagen. DO NOT EDIT.
$defs:
  metrics_config:
    description: MetricsConfig provides config for slabinfo metrics.
    type: object
    properties:
      slab.dentry_cache.active_objects:
        description: "SlabDentryCacheActiveObjectsMetricConfig provides config for the slab.dentry_cache.active_objects metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      slab.dentry_cache.objsize:
        description: "SlabDentryCacheObjsizeMetricConfig provides config for the slab.dentry_cache.objsize metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      slab.nfs_inode_cache.active_objects:
        description: "SlabNfsInodeCacheActiveObjectsMetricConfig provides config for the slab.nfs_inode_cache.active_objects metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      slab.nfs_inode_cache.objsize:
        description: "SlabNfsInodeCacheObjsizeMetricConfig provides config for the slab.nfs_inode_cache.objsize metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
  metrics_builder_config:
    description: MetricsBuilderConfig is a configuration for slabinfo metrics builder.
    type: object
    properties:
      metrics:
        $ref: metrics_config
