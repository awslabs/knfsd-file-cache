# Code generated by mdatagen. DO NOT EDIT.
$defs:
  metrics_config:
    description: MetricsConfig provides config for nfsd metrics.
    type: object
    properties:
      nfs.packets.arrived:
        description: "NfsPacketsArrivedMetricConfig provides config for the nfs.packets.arrived metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nfs.packets.deferred:
        description: "NfsPacketsDeferredMetricConfig provides config for the nfs.packets.deferred metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nfs.sockets.enqueued:
        description: "NfsSocketsEnqueuedMetricConfig provides config for the nfs.sockets.enqueued metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nfs.threads:
        description: "NfsThreadsMetricConfig provides config for the nfs.threads metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nfs.threads.timedout:
        description: "NfsThreadsTimedoutMetricConfig provides config for the nfs.threads.timedout metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nfs.threads.woken:
        description: "NfsThreadsWokenMetricConfig provides config for the nfs.threads.woken metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
  metrics_builder_config:
    description: MetricsBuilderConfig is a configuration for nfsd metrics builder.
    type: object
    properties:
      metrics:
        $ref: metrics_config
