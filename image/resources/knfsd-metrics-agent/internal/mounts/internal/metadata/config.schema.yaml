# Code generated by mdatagen. DO NOT EDIT.
$defs:
  metrics_config:
    description: MetricsConfig provides config for mounts metrics.
    type: object
    properties:
      nfs.mount.operation.errors:
        description: "NfsMountOperationErrorsMetricConfig provides config for the nfs.mount.operation.errors metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nfs.mount.operation.major_timeouts:
        description: "NfsMountOperationMajorTimeoutsMetricConfig provides config for the nfs.mount.operation.major_timeouts metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nfs.mount.operation.received_bytes:
        description: "NfsMountOperationReceivedBytesMetricConfig provides config for the nfs.mount.operation.received_bytes metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nfs.mount.operation.requests:
        description: "NfsMountOperationRequestsMetricConfig provides config for the nfs.mount.operation.requests metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nfs.mount.operation.sent_bytes:
        description: "NfsMountOperationSentBytesMetricConfig provides config for the nfs.mount.operation.sent_bytes metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nfs.mount.ops_per_second:
        description: "NfsMountOpsPerSecondMetricConfig provides config for the nfs.mount.ops_per_second metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nfs.mount.read_bytes:
        description: "NfsMountReadBytesMetricConfig provides config for the nfs.mount.read_bytes metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nfs.mount.read_exe:
        description: "NfsMountReadExeMetricConfig provides config for the nfs.mount.read_exe metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nfs.mount.read_rtt:
        description: "NfsMountReadRttMetricConfig provides config for the nfs.mount.read_rtt metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nfs.mount.rpc_backlog:
        description: "NfsMountRPCBacklogMetricConfig provides config for the nfs.mount.rpc_backlog metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nfs.mount.write_bytes:
        description: "NfsMountWriteBytesMetricConfig provides config for the nfs.mount.write_bytes metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nfs.mount.write_exe:
        description: "NfsMountWriteExeMetricConfig provides config for the nfs.mount.write_exe metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nfs.mount.write_rtt:
        description: "NfsMountWriteRttMetricConfig provides config for the nfs.mount.write_rtt metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
  metrics_builder_config:
    description: MetricsBuilderConfig is a configuration for mounts metrics builder.
    type: object
    properties:
      metrics:
        $ref: metrics_config
