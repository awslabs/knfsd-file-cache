# Code generated by mdatagen. DO NOT EDIT.
$defs:
  metrics_config:
    description: MetricsConfig provides config for fscache metrics.
    type: object
    properties:
      fscache.acquire.ok:
        description: "FscacheAcquireOkMetricConfig provides config for the fscache.acquire.ok metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      fscache.acquire.oom:
        description: "FscacheAcquireOomMetricConfig provides config for the fscache.acquire.oom metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      fscache.acquire.requests:
        description: "FscacheAcquireRequestsMetricConfig provides config for the fscache.acquire.requests metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      fscache.cookies.data:
        description: "FscacheCookiesDataMetricConfig provides config for the fscache.cookies.data metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      fscache.cookies.volume:
        description: "FscacheCookiesVolumeMetricConfig provides config for the fscache.cookies.volume metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      fscache.cookies.volume_collisions:
        description: "FscacheCookiesVolumeCollisionsMetricConfig provides config for the fscache.cookies.volume_collisions metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      fscache.cookies.volume_oom:
        description: "FscacheCookiesVolumeOomMetricConfig provides config for the fscache.cookies.volume_oom metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      fscache.invalidations:
        description: "FscacheInvalidationsMetricConfig provides config for the fscache.invalidations metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      fscache.io.misfit:
        description: "FscacheIoMisfitMetricConfig provides config for the fscache.io.misfit metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      fscache.io.read:
        description: "FscacheIoReadMetricConfig provides config for the fscache.io.read metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      fscache.io.write:
        description: "FscacheIoWriteMetricConfig provides config for the fscache.io.write metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      fscache.lru.count:
        description: "FscacheLruCountMetricConfig provides config for the fscache.lru.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      fscache.lru.dropped:
        description: "FscacheLruDroppedMetricConfig provides config for the fscache.lru.dropped metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      fscache.lru.expired:
        description: "FscacheLruExpiredMetricConfig provides config for the fscache.lru.expired metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      fscache.lru.removed:
        description: "FscacheLruRemovedMetricConfig provides config for the fscache.lru.removed metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      fscache.nospace.create:
        description: "FscacheNospaceCreateMetricConfig provides config for the fscache.nospace.create metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      fscache.nospace.cull:
        description: "FscacheNospaceCullMetricConfig provides config for the fscache.nospace.cull metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      fscache.nospace.write:
        description: "FscacheNospaceWriteMetricConfig provides config for the fscache.nospace.write metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      fscache.relinquish.drop:
        description: "FscacheRelinquishDropMetricConfig provides config for the fscache.relinquish.drop metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      fscache.relinquish.requests:
        description: "FscacheRelinquishRequestsMetricConfig provides config for the fscache.relinquish.requests metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      fscache.relinquish.retire:
        description: "FscacheRelinquishRetireMetricConfig provides config for the fscache.relinquish.retire metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      fscache.updates.requests:
        description: "FscacheUpdatesRequestsMetricConfig provides config for the fscache.updates.requests metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      fscache.updates.resize:
        description: "FscacheUpdatesResizeMetricConfig provides config for the fscache.updates.resize metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      fscache.updates.resize_skipped:
        description: "FscacheUpdatesResizeSkippedMetricConfig provides config for the fscache.updates.resize_skipped metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      netfs.cache_read.done:
        description: "NetfsCacheReadDoneMetricConfig provides config for the netfs.cache_read.done metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      netfs.cache_read.failed:
        description: "NetfsCacheReadFailedMetricConfig provides config for the netfs.cache_read.failed metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      netfs.cache_read.requests:
        description: "NetfsCacheReadRequestsMetricConfig provides config for the netfs.cache_read.requests metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      netfs.cache_write.done:
        description: "NetfsCacheWriteDoneMetricConfig provides config for the netfs.cache_write.done metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      netfs.cache_write.failed:
        description: "NetfsCacheWriteFailedMetricConfig provides config for the netfs.cache_write.failed metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      netfs.cache_write.requests:
        description: "NetfsCacheWriteRequestsMetricConfig provides config for the netfs.cache_write.requests metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      netfs.download.done:
        description: "NetfsDownloadDoneMetricConfig provides config for the netfs.download.done metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      netfs.download.failed:
        description: "NetfsDownloadFailedMetricConfig provides config for the netfs.download.failed metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      netfs.download.instead:
        description: "NetfsDownloadInsteadMetricConfig provides config for the netfs.download.instead metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      netfs.download.requests:
        description: "NetfsDownloadRequestsMetricConfig provides config for the netfs.download.requests metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      netfs.objects.folio_queue:
        description: "NetfsObjectsFolioQueueMetricConfig provides config for the netfs.objects.folio_queue metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      netfs.objects.read_reqs:
        description: "NetfsObjectsReadReqsMetricConfig provides config for the netfs.objects.read_reqs metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      netfs.objects.subreqs:
        description: "NetfsObjectsSubreqsMetricConfig provides config for the netfs.objects.subreqs metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      netfs.objects.write_conflicts:
        description: "NetfsObjectsWriteConflictsMetricConfig provides config for the netfs.objects.write_conflicts metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      netfs.reads.direct:
        description: "NetfsReadsDirectMetricConfig provides config for the netfs.reads.direct metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      netfs.reads.folio:
        description: "NetfsReadsFolioMetricConfig provides config for the netfs.reads.folio metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      netfs.reads.readahead:
        description: "NetfsReadsReadaheadMetricConfig provides config for the netfs.reads.readahead metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      netfs.reads.single:
        description: "NetfsReadsSingleMetricConfig provides config for the netfs.reads.single metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      netfs.reads.write_begin:
        description: "NetfsReadsWriteBeginMetricConfig provides config for the netfs.reads.write_begin metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      netfs.reads.write_zskip:
        description: "NetfsReadsWriteZskipMetricConfig provides config for the netfs.reads.write_zskip metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      netfs.retries.read_req:
        description: "NetfsRetriesReadReqMetricConfig provides config for the netfs.retries.read_req metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      netfs.retries.read_subreq:
        description: "NetfsRetriesReadSubreqMetricConfig provides config for the netfs.retries.read_subreq metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      netfs.retries.write_req:
        description: "NetfsRetriesWriteReqMetricConfig provides config for the netfs.retries.write_req metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      netfs.retries.write_subreq:
        description: "NetfsRetriesWriteSubreqMetricConfig provides config for the netfs.retries.write_subreq metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      netfs.upload.done:
        description: "NetfsUploadDoneMetricConfig provides config for the netfs.upload.done metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      netfs.upload.failed:
        description: "NetfsUploadFailedMetricConfig provides config for the netfs.upload.failed metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      netfs.upload.requests:
        description: "NetfsUploadRequestsMetricConfig provides config for the netfs.upload.requests metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      netfs.wblock.skip:
        description: "NetfsWblockSkipMetricConfig provides config for the netfs.wblock.skip metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      netfs.wblock.wait:
        description: "NetfsWblockWaitMetricConfig provides config for the netfs.wblock.wait metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      netfs.writes.buffered:
        description: "NetfsWritesBufferedMetricConfig provides config for the netfs.writes.buffered metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      netfs.writes.copy_to_cache:
        description: "NetfsWritesCopyToCacheMetricConfig provides config for the netfs.writes.copy_to_cache metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      netfs.writes.direct:
        description: "NetfsWritesDirectMetricConfig provides config for the netfs.writes.direct metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      netfs.writes.pages:
        description: "NetfsWritesPagesMetricConfig provides config for the netfs.writes.pages metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      netfs.writes.writethrough:
        description: "NetfsWritesWritethroughMetricConfig provides config for the netfs.writes.writethrough metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      netfs.zero_ops.short:
        description: "NetfsZeroOpsShortMetricConfig provides config for the netfs.zero_ops.short metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      netfs.zero_ops.skip:
        description: "NetfsZeroOpsSkipMetricConfig provides config for the netfs.zero_ops.skip metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      netfs.zero_ops.zero:
        description: "NetfsZeroOpsZeroMetricConfig provides config for the netfs.zero_ops.zero metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
  metrics_builder_config:
    description: MetricsBuilderConfig is a configuration for fscache metrics builder.
    type: object
    properties:
      metrics:
        $ref: metrics_config
