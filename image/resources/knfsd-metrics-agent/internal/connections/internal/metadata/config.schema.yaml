# Code generated by mdatagen. DO NOT EDIT.
$defs:
  metrics_config:
    description: MetricsConfig provides config for connections metrics.
    type: object
    properties:
      nfs.clients:
        description: "NfsClientsMetricConfig provides config for the nfs.clients metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nfs.connections:
        description: "NfsConnectionsMetricConfig provides config for the nfs.connections metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
  metrics_builder_config:
    description: MetricsBuilderConfig is a configuration for connections metrics builder.
    type: object
    properties:
      metrics:
        $ref: metrics_config
